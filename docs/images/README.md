# Screenshots for portfolio

After running `make run`, capture screenshots from:

- `outputs/map.html` → save as `map.png`
- `outputs/dashboard.html` → save as `dashboard.png`

Then embed them in README or your CV:

```markdown
![Zone map](docs/images/map.png)
```
